Original prompt: отлично. этап 1

- Started Stage 1: restore frontend screens/routing and keep apps/web build green.
- Loaded develop-web-game skill and began baseline inspection.
- Added in-memory mock backend (`src/api/mockBackend.ts`) and wired core API modules (auth/user/game/upgrade/inventory/shop/leaderboard/referral) through `withMockApi`.
- Replaced `App.tsx` with routed shell (Hub/Hunt/Upgrade/Inventory/Shop/Leaderboard/Referral), bottom nav, and Playwright bridge (`window.render_game_to_text`, `window.advanceTime`).
- Fixed key frontend wiring issues: Hub default export, TelegramInit hook import path, and useHunt import paths.
- Added global profile bootstrap in AppShell so non-Hub routes receive profile/stats and no longer show 0-values by default.

Testing and verification:
- `npm run build --prefix apps/web` passes.
- Playwright client runs completed against:
  - `/hub`
  - `/hunt` (idle)
  - `/hunt` after click `Начать охоту` (active enemy/action buttons)
  - `/upgrade`
  - `/inventory`
  - `/shop`
  - `/leaderboard`
  - `/referral`
- Artifacts stored under `output/web-game/stage1-*`.
- No `errors-*.json` files were generated by the client.

TODO / next agent suggestions:
- Decide whether to keep `VITE_USE_MOCK_API=true` default for Stage 1 and document this in `.env.example`.
- Add explicit UI entry points to Leaderboard/Referral from bottom nav (currently available from Hub/menu and direct routes).
- For Stage 2+, switch each API module from mock-first to real API-first behind a feature flag.
- Updated project docs for Stage 1 baseline (README/STATUS/QUICKSTART) and marked Stage 1 checklist items done in DEVELOPMENT_PLAN.
